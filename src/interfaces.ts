export interface Size {
    height: number;
    width: number;
}

export interface CanvasSettings {
    selector: string;
    size?: Size;
}

export interface Position {
    x: number;
    y: number;
}
